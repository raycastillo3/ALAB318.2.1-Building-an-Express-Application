mixin createPostForm(userLoggedIn)
    .postFormContainer
        .userImageContainer
            img(src=userLoggedIn.profilePicture, alt="profile pic")
        .textareaContainer
            textarea#postTextarea(placeholder="Discussions")
            .buttonContainer
                button#submitPostButton(disabled="") Publish